{
  "name": "pearcore",
  "version": "0.1.0",
  "description": "Pearcore — Reusable application framework (PearTree, Demo, and more)",
  "private": true,
  "workspaces": [
    "pearcore",
    "peartree",
    "demo"
  ],
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon",
    "bundle": "node scripts/build-bundle.mjs --app peartree",
    "bundle:peartree": "node scripts/build-bundle.mjs --app peartree",
    "bundle:demo": "node scripts/build-bundle.mjs --app demo"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "esbuild": "^0.27.4"
  }
}
