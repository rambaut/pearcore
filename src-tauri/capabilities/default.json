{
  "identifier": "default",
  "description": "Default capability for PearTree – grants core APIs to the main window.",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:allow-open"
  ]
}
